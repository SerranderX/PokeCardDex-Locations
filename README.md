- Run the build project in watch mode:
    radle build --continuous

- Run the project in watch mode:
    gradle bootRun

# For developement mode in the project, you need to run both comands describes above